#include <stdio.h>

int main(){
    int amount;
    // &->Store value inside this varaible momory location
    printf("Enter the amount:");
    scanf("%d",&amount);
    printf("User Entered amount is: %d",amount);
    return 0;
}